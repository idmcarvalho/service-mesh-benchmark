# Oracle Cloud Infrastructure Configuration
# Copy this file to terraform.tfvars and fill in your values
# NEVER commit terraform.tfvars to version control

# Authentication - Get these from OCI Console
tenancy_ocid     = "ocid1.tenancy.oc1..aaaaaaaaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
user_ocid        = "ocid1.user.oc1..aaaaaaaaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
fingerprint      = "xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx"
private_key_path = "~/.oci/oci_api_key.pem"
compartment_ocid = "ocid1.compartment.oc1..aaaaaaaaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Region
region = "us-ashburn-1"

# SSH Configuration
ssh_public_key_path = "~/.ssh/id_rsa.pub"

# Security - CRITICAL: MUST set these to your actual IP address
# Get your IP: curl ifconfig.me
# SECURITY WARNING: These variables are now REQUIRED and cannot be 0.0.0.0/0
# Single IP format: "203.0.113.42/32"
# IP range format: "203.0.113.0/24"
allowed_ssh_cidr      = "YOUR.IP.ADDRESS.HERE/32"  # REQUIRED - Cannot be 0.0.0.0/0
allowed_api_cidr      = "YOUR.IP.ADDRESS.HERE/32"  # REQUIRED - Cannot be 0.0.0.0/0
allowed_nodeport_cidr = "YOUR.IP.ADDRESS.HERE/32"  # REQUIRED - Cannot be 0.0.0.0/0

# Instance Configuration (Free Tier: VM.Standard.A1.Flex)
instance_shape     = "VM.Standard.A1.Flex"
instance_ocpus     = 2
instance_memory_gb = 12

# Worker Nodes (Adjust based on free tier limits)
worker_count      = 2
worker_ocpus      = 1
worker_memory_gb  = 6

# Test Type
test_type = "benchmark"  # Options: istio, cilium, linkerd, baseline, benchmark

# Network
vcn_cidr    = "10.0.0.0/16"
subnet_cidr = "10.0.1.0/24"

# Tags
project_tags = {
  Project     = "ServiceMeshBenchmark"
  Environment = "Research"
  ManagedBy   = "Terraform"
  Owner       = "YourName"
}
