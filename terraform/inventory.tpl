# Ansible Inventory - Generated by Terraform
# Do not edit manually - this file is auto-generated

all:
  hosts:
    benchmark_instance:
      ansible_host: ${instance_ip}
      ansible_user: ${instance_user}
      ansible_ssh_private_key_file: ${ssh_private_key}
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
      ansible_python_interpreter: /usr/bin/python3

  vars:
    # Environment
    environment: ${environment}

    # Domain configuration
    domain_name: ${domain_name}
    use_ssl: ${domain_name != "" ? "true" : "false"}

    # Application paths
    app_directory: /opt/service-mesh-benchmark

    # Docker configuration
    docker_compose_file: docker-compose.prod.yml

    # Application URLs (when using IP)
    frontend_url: "http://${instance_ip}:3000"
    api_url: "http://${instance_ip}:8000"
    prometheus_url: "http://${instance_ip}:9090"
    grafana_url: "http://${instance_ip}:3001"

  children:
    benchmark_servers:
      hosts:
        benchmark_instance:
